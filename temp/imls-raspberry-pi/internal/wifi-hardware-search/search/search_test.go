package search

import (
	"testing"
)

func TestGetSearchStrings(t *testing.T) {
	// embedded := GetSearchStrings()
	// local, err := readLines("models.txt")

	// if err != nil {
	// 	t.Error("error reading local models file. Panic!")
	// }

	// if len(embedded) != len(local) {
	// 	fmt.Println("embedded", len(embedded), "local", len(local))
	// 	for _, line := range embedded {
	// 		fmt.Println("e", line)
	// 	}
	// 	for _, line := range local {
	// 		fmt.Println("l", line)
	// 	}
	// 	t.Error("model arrays are different lengths. Inconceivable!")
	// }

	// for ndx := range embedded {
	// 	if embedded[ndx] != local[ndx] {
	// 		t.Error("mismatch in model arrays. Such bad!")
	// 	}
	// }
}
